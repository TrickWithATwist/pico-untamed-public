//variable that allows player to interact with object
allow = 1;

//variable that keeps track if object has been clicked or not to avoid 
//multiple textboxes from showing up if the player spam clicks
clicked = 0;