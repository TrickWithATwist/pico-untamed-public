//establish variables for sprite scaling when mouse hovers or leaves
start_scale = .8;
scale_max = 1;

//speed it takes for sprite scale to stretch 
stretch_speed = .2;

//set initial color blend for button 
image_blend = c_gray;

//variable that keeps track of button pressed
buttonpress = 0;

//variable that allows player to interact with object
buttonallow = 1;